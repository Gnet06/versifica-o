DENTRO DA PASTA DO PROJETO
git init
git add .
git commit -m "inicio do projeto"
git branch -M main
git remote add origin (LINK DO SEU REPOSITORIO).git
git push -u origin main

COPIA O CODIGO DO ARQUIVO 0.1.0 DENTRO DE PRECOMMITS E JOGA NO MAIN.PY (copia ate a linha 23 só)
git checkout -b develop
git add .
git commit -m "criando variáveis e formatando dados"
git push -u origin develop

AINDA COPIANDO DO 0.1.0 PODE APAGAR AS LINHAS (4-23) E COPIAR TUDO DE NOVO E JOGAR NA MAIN.PY
git checkout -b feature
git add .
git commit -m "adicionando tratamento de erros"
git push -u origin feature

git checkout develop
git merge feature

git checkout -b release
git merge develop
git tag -a v0.1.0 -m "iniciando versionamento"
git push origin --tags

git checkout main
git merge release

COPIA DO 0.2.0 PRO MAIN.PY
git checkout develop
git add .
git commit -m "iniciando as classificações"
git push -u origin develop

git checkout release
git merge develop
git tag -a v0.2.0 -m "código com classificação de nível"
git push origin --tags

git checkout main
git merge release

COPIA DO 0.3.0 PRO MAIN.PY
git checkout develop
git add .
git commit -m "adicionando saída de dados"
git push -u origin develop

git checkout release
git merge develop
git tag -a v0.3.0 -m "saída de dados com os respectivos níveis"
git push origin --tags

git checkout main
git merge release

COPIA DO 1.0.0 PRO MAIN.PY
git checkout feature
git add .
git commit -m "adicionando a opção de continuidade ao código"
git push -u origin feature

git checkout develop
git merge feature

git checkout release
git merge develop
git tag -a v1.0.0 -m "código com loop"
git push origin --tags

git checkout main
git merge release










